[tool.poetry]
name = "repl-nix-workspace"
version = "0.1.0"
description = "Add your description here"
authors = ["Your Name <yourname@example.com>"]
# Specify the supported Python version
python = ">=3.9,<3.13"

[tool.poetry.dependencies]
python = ">=3.9,<3.13"  # Add this line to explicitly specify Python version
flask-login = ">=0.6.3"
flask = ">=3.1.0"
numpy = ">=2.2.5"
opencv-python = ">=4.11.0.86"
pandas = ">=2.2.3"
pillow = ">=11.2.1"
pyttsx3 = ">=2.98"
ttkthemes = ">=3.2.2"
flask-wtf = ">=1.2.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
